import { PageState } from "@/components/PageState";
export default function NotFound() {
  return <PageState eyebrow="404 / Не найдено" title="Такой страницы нет" text="Возможно, ссылка устарела. Вернитесь к курсам, чтобы продолжить обучение." />;
}
