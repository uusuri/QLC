server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/qlc_database
    username: uusuri
    password: 101526331
    driver-class-name: org.postgresql.Driver
  jpa: 
    hibernate:
      ddl-auto: validate
      show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

  flyway:
    enabled: true
    locations: classpath:db/migration
