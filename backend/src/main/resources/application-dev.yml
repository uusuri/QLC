server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/qlc_db
    username: postgres
    password: dev_password
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379

  flyway:
    enabled: true
    baseline-on-migrate: true
