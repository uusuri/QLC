spring:
  application:
    name: qlc-backend
  profiles:
    active: dev
  data: 
    redis:
      host: localhost
      port: 6379

  jackson:
    default-property-inclusion: non_null

app:
  submission:
    max-source-size: 65535
    queue-name: "queue:submissions"
